input = int(input())

nextnum = input + 1;
previousnum = input - 1;

print("The next number for the number %d is %d." %(input, nextnum))
print("The previous number for the number %d is %d." %(input, previousnum))
